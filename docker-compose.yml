services:
  backend:
    build: ./backend
    command: ["uvicorn", "ogi.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "${OGI_PORT:-8000}:8000"
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - ./plugins:/app/plugins
    environment:
      - OGI_DATABASE_URL=postgresql://postgres:postgres@db:5432/ogi
      - OGI_USE_SQLITE=false
      - OGI_REDIS_URL=redis://redis:6379/0
      - OGI_RUN_DB_MIGRATIONS=true
      - OGI_AUTO_RUN_MIGRATIONS=false
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5).status == 200 else 1)",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  worker:
    build: ./backend
    command: ["python", "-m", "ogi.worker.run_worker"]
    env_file: .env
    depends_on:
      backend:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - ./plugins:/app/plugins
    environment:
      - OGI_DATABASE_URL=postgresql://postgres:postgres@db:5432/ogi
      - OGI_USE_SQLITE=false
      - OGI_REDIS_URL=redis://redis:6379/0
      - OGI_RUN_DB_MIGRATIONS=false
      - OGI_AUTO_RUN_MIGRATIONS=false

  frontend:
    build:
      context: ./frontend
      args:
        VITE_SUPABASE_URL: ${OGI_SUPABASE_URL:-}
        VITE_SUPABASE_ANON_KEY: ${OGI_SUPABASE_ANON_KEY:-}
    ports:
      - "${OGI_FRONTEND_PORT:-3000}:80"
    depends_on:
      backend:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ogi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
